interface ExtraParams {
    gas?: string;
    gasPrice: string;
    value: string;
    data: string;
}
export declare function sendRawTransaction(url: string, privateKeyOrKnownAddress: string, to: string, { gas, ...extraParams }: ExtraParams): Promise<string>;
export {};
